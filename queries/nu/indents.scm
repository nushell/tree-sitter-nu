[
  (pipeline)
  (pipe_element)
  (expr_parenthesized)
  (val_record)
  (val_list)
  (val_closure)
  (val_table)
  (block)
] @indent.begin

[
  "}"
  "]"
  ")"
] @indent.end

[
  "}"
  "]"
  ")"
] @indent.branch

(comment) @indent.auto
